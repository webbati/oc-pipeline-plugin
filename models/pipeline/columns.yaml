# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: Pipeline
        searchable: true
    stage_count:
        label: Stages
        type: text
    is_enabled:
        label: Enabled
        type: switch